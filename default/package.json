{
  "type": "module",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "lint:prettier": "prettier -c . --cache",
    "check-types": "tsc --noEmit --project tsconfig.lint.json",
    "dev": "flareforge dev",
    "build": "flareforge build",
    "db:drop": "drizzle-kit drop",
    "db:generate": "drizzle-kit generate",
    "db:apply": "wrangler d1 migrations apply DB --local",
    "db:apply-prod": "wrangler d1 migrations apply DB --remote",
    "stripe:listen": "stripe listen --forward-to localhost:5173/webhook/stripe"
  },
  "postcss": {
    "plugins": {
      "@tailwindcss/postcss": {},
      "autoprefixer": {}
    }
  },
  "dependencies": {},
  "devDependencies": {
    "@flareforge/dev": "*"
  }
}
